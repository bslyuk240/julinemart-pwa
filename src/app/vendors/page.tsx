import VendorsDirectory from './vendors-directory';

export const metadata = {
  title: 'Local Stores | JulineMart',
  description: 'Discover trusted local businesses near you on JulineMart.',
};

export default function VendorsPage() {
  return <VendorsDirectory />;
}
